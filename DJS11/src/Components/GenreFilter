import React from 'react';

const GenreFilter = () => {
    return (
        <div>
            <h3>Filter by Genre</h3>
            {/* Add code for genre filtering */}
        </div>
    );
};

export default GenreFilter;
